<!-- Generated by python3 -m benchmarks --write. Do not edit by hand. -->

# 模型列表

Studio 能跑的全部配方。**契约**在 `catalog/*.json`（标题、kind、GPU、工作流）。**权重 / 节点 / 实测**在 [`benchmarks/models.json`](https://github.com/xiaoqianran/modal-ashleykza-comfyui/blob/main/benchmarks/models.json)。加配方时三个 JSON（example + lock + catalog）之外，还要在 overlay 里加同一 `id`，然后：

```bash
python3 -m benchmarks --write
```

本页由上面这条命令生成。数据更新日期 **2026-08-14**。

## 共用环境

| 项 | 值 |
|---|---|
| Image | ashleykleynhans/comfyui cu128-py312 |
| ComfyUI | **构建时解析最新 cu128-py312-v*** |
| 平台 | Modal（profile `weiranzhiqian`） |
| 计时 | 从 POST /prompt 到 /history 出现 success 的客户端墙钟。首张常含装进 VRAM 或同卡换模。 |
| 空闲缩容 | **5 秒**（`modal serve` 会挡住） |

测试默认 **L40S**（FLUX.2 / TRELLIS.2 只能 RTX-PRO-6000）。正式推理 **RTX-PRO-6000**。不要用 T4。测完停 serve。

## 总览

| 配方 | 标题 | kind | 测试 GPU | 正式 GPU | 步数 | 输出 | 权重 | 实测 |
|---|---|---|---|---|---:|---|---|---|
| `z-image` | Z-Image | t2i | **L40S** | RTX-PRO-6000 | 25 | 1024² png | — | 待补测 |
| `cosmos3-edge` | Cosmos3-Edge | t2v | **L40S** | RTX-PRO-6000 | 35 | 832×480 / 93 帧 / 24fps mp4 | 3.1 GB | L40S 49.1s |
| `cosmos3-nano` | Cosmos3-Nano | t2v | **L40S** | RTX-PRO-6000 | 35 | 832×480 / 93 帧 / 24fps mp4 | 12.4 GB | L40S 133.2s |
| `cosmos3-super` | Cosmos3-Super | t2v | **L40S** | RTX-PRO-6000 | 35 | 832×480 / 93 帧 / 24fps mp4 | 46.8 GB | 待补测 |
| `cosmos3-super-image2video` | Cosmos3-Super-Image2Video | i2v | **L40S** | RTX-PRO-6000 | 35 | 832×480 / 93 帧 / 16fps mp4 | 46.7 GB | L40S 517.6s |
| `cosmos3-super-image2video-4step` | Cosmos3-Super-Image2Video-4Step | i2v | **L40S** | RTX-PRO-6000 | 4 | 832×480 / 93 帧 / 16fps mp4 | 46.7 GB | L40S 50.8s |
| `cosmos3-super-text2image` | Cosmos3-Super-Text2Image | t2i | **L40S** | RTX-PRO-6000 | 35 | 1024² png | 46.8 GB | L40S 245.6s |
| `cosmos3-super-text2image-4step` | Cosmos3-Super-Text2Image-4Step | t2i | **L40S** | RTX-PRO-6000 | 4 | 1024² png | 46.7 GB | L40S 66s |
| `flux2-dev` | FLUX.2 [dev] | t2i | **RTX-PRO-6000** | RTX-PRO-6000 | — | 1024² png | 71 GB | RTX-PRO-6000 首张 144s / 热 15.4s |
| `hunyuan3d-2.1` | Hunyuan3D 2.1 | i23d | **L40S** | RTX-PRO-6000 | 30 | GLB | 4.9 GB | L40S 43.9s |
| `ideogram4` | Ideogram 4 | t2i | **L40S** | RTX-PRO-6000 | 20 | 1024² png | 38 GB | 已 hydrate，未记时 |
| `krea2-turbo` | Krea-2 Turbo | t2i | **L40S** | RTX-PRO-6000 | 8 | 1024² png | 17 GB | RTX-PRO-6000 首张 39.2s / 热 8.9s |
| `pixal3d` | Pixal3D | i23d | **L40S** | RTX-PRO-6000 | — | GLB | — | 待补测 |
| `qwen-image-2512` | Qwen-Image-2512 | t2i | **L40S** | RTX-PRO-6000 | 50 | 1328² png | 32 GB | RTX-PRO-6000 首张 77.6s / 热 53.9s |
| `qwen-image-2512-lightning` | Qwen-Image-2512 Lightning | t2i | **L40S** | RTX-PRO-6000 | 8 | 1328² png | 34 GB | 已 hydrate，未记时 |
| `trellis2` | TRELLIS.2 | i23d | **RTX-PRO-6000** | RTX-PRO-6000 | — | GLB | 16 GB | RTX-PRO-6000 208.3s |
| `triposplat` | TripoSplat | i23d | **L40S** | RTX-PRO-6000 | — | SPZ + GLB | — | L40S 首张 53.7s / 热 30.1s |
| `z-image-turbo` | Z-Image-Turbo | t2i | **L40S** | RTX-PRO-6000 | 8 | 1024² png | — | 已 hydrate，未记时 |

kind：`t2i` 文生图，`t2v` 文生视频，`i2v` 图生视频，`i23d` 图生 3D。

## 文生图

### Z-Image (`z-image`)

文生图。权重来自 Comfy-Org，锁内无自定义节点。测试默认 L40S，正式推理 RTX-PRO-6000。

| 项 | 值 |
|---|---|
| 工作流 | `examples/z-image-base.json` |
| 锁 | `examples/z-image-base.lock.json` |
| 模式 | `graph` |
| 量化 | bf16 |
| 节点 | core |
| 权重 | —；显存 — |
| 共用 | `z-image-turbo` |
| 实测状态 | `pending` |
| 实测 GPU / 耗时 | 待补测 |
| 备注 | Studio 默认配方。测试卡已改成 L40S，尚未正式记时。 |

### Cosmos3-Super-Text2Image (`cosmos3-super-text2image`)

64B 文生图（1024² / 1 帧）。ConvRot 没有 T2I 专用 int4，64B bf16 主机内存约 240GB；本配方用同架构的 Super int4 做 1 帧图，才能在 L40S 跑。测试 L40S，正式 RTX-PRO-6000。不要用 T4。

| 项 | 值 |
|---|---|
| 工作流 | `examples/cosmos3-super-text2image.json` |
| 锁 | `examples/cosmos3-super-text2image.lock.json` |
| 模式 | `workflow` |
| 量化 | int4-convrot |
| 节点 | ComfyUI-Cosmos3 |
| 权重 | 46.8 GB；显存 8 GB；主机内存约 63 GB |
| 共用 | `cosmos3-super` |
| 实测状态 | `recorded` · PR #34 · 2026-08-14 |
| 实测 GPU / 耗时 | L40S 245.6s |
| 冒烟产物 | 1024² |

### Cosmos3-Super-Text2Image-4Step (`cosmos3-super-text2image-4step`)

64B 文生图蒸馏 4 步（1024² / 1 帧）。ConvRot 没有 T2I-4Step 专用 int4；官方 bf16 约 120GB。本配方用同目录的 Super-Image2Video-4Step int4 做 1 帧（VAE/tokenizer 与官方 T2I-4Step 相同），euler + cfg 1.0。测试 L40S，正式 RTX-PRO-6000。不要用 T4。

| 项 | 值 |
|---|---|
| 工作流 | `examples/cosmos3-super-text2image-4step.json` |
| 锁 | `examples/cosmos3-super-text2image-4step.lock.json` |
| 模式 | `workflow` |
| 量化 | int4-convrot |
| 节点 | ComfyUI-Cosmos3 |
| 权重 | 46.7 GB；显存 8 GB；主机内存约 63 GB |
| 共用 | `cosmos3-super-image2video-4step` |
| 实测状态 | `recorded` · PR #35 · 2026-08-14 |
| 实测 GPU / 耗时 | L40S 66s |
| 冒烟产物 | 1024² |

### FLUX.2 [dev] (`flux2-dev`)

官方文生图模板（fp8 mixed + Mistral TE）。约 71GB，L40S 放不下，测试和正式都是 RTX-PRO-6000。

| 项 | 值 |
|---|---|
| 工作流 | `examples/flux2-dev-t2i.json` |
| 锁 | `examples/flux2-dev-t2i.lock.json` |
| 模式 | `workflow` |
| 量化 | fp8-mixed |
| 节点 | core |
| 权重 | 71 GB；显存 70 GB |
| 共用 | — |
| 实测状态 | `recorded` · PR #21 · 2026-08-13 |
| 实测 GPU / 耗时 | RTX-PRO-6000 首张 144s / 热 15.4s |
| 冒烟产物 | 1024² / 20 步 |
| 备注 | 5/5 success。首张冷加载；热显存 15.34–15.38s。 |

| 样本 | 秒 |
|---|---:|
| 青瓷壶 (冷加载 ~70GB VRAM) | 144s |
| 雨夜红风衣 | 15.3s |
| 梯田日出 | 15.4s |
| 窗边橘猫 | 15.4s |
| 青花瓷荔枝汽水 | 15.3s |

### Ideogram 4 (`ideogram4`)

开源 Ideogram 4.0 文生图（fp8）。双 DiT + Qwen3-VL + Gemma4 + Flux2 VAE，约 38GB。测试 L40S，正式 RTX-PRO-6000。支持自然语言或 JSON 提示词。

| 项 | 值 |
|---|---|
| 工作流 | `examples/ideogram4-t2i.json` |
| 锁 | `examples/ideogram4-t2i.lock.json` |
| 模式 | `workflow` |
| 量化 | fp8 |
| 节点 | core (Ideogram4Scheduler / DualModelGuider 需新 ComfyUI) |
| 权重 | 38 GB；显存 38 GB |
| 共用 | — |
| 实测状态 | `hydrated` · PR #32 · 2026-08-14 |
| 实测 GPU / 耗时 | 已 hydrate，未记时 |
| 备注 | hydrate 已写入。Ashley 0.32.0 可能缺 Ideogram4 节点，未排队出图。不要升级 GPU Image。 |

### Krea-2 Turbo (`krea2-turbo`)

官方文生图模板（fp8 Turbo，8 步）。约 17GB；测试默认 L40S，正式推理 RTX-PRO-6000。

| 项 | 值 |
|---|---|
| 工作流 | `examples/krea2-turbo-t2i.json` |
| 锁 | `examples/krea2-turbo-t2i.lock.json` |
| 模式 | `workflow` |
| 量化 | fp8 |
| 节点 | core |
| 权重 | 17 GB；显存 17.4 GB |
| 共用 | — |
| 实测状态 | `recorded` · PR #24 · 2026-08-13 |
| 实测 GPU / 耗时 | RTX-PRO-6000 首张 39.2s / 热 8.9s |
| 冒烟产物 | 1024² / 8 步 |
| 备注 | 5/5 success，约 17.4GB 显存。prompt enhance 开着，后续张 8.9–19.7s 不稳。 |

| 样本 | 秒 |
|---|---:|
| 青瓷壶 (冷加载) | 39.2s |
| 雨夜红风衣 | 19.7s |
| 梯田日出 | 9s |
| 窗边橘猫 | 8.9s |
| 青花瓷荔枝汽水 | 17.6s |

### Qwen-Image-2512 (`qwen-image-2512`)

官方文生图模板（fp8 + Lightning LoRA，默认关）。约 32GB；测试默认 L40S，正式推理 RTX-PRO-6000。

| 项 | 值 |
|---|---|
| 工作流 | `examples/qwen-image-2512.json` |
| 锁 | `examples/qwen-image-2512.lock.json` |
| 模式 | `workflow` |
| 量化 | fp8 |
| 节点 | core |
| 权重 | 32 GB；显存 32 GB |
| 共用 | `qwen-image-2512-lightning` |
| 实测状态 | `recorded` · PR #21 · 2026-08-13 |
| 实测 GPU / 耗时 | RTX-PRO-6000 首张 77.6s / 热 53.9s |
| 冒烟产物 | 1328² / 50 步 |
| 备注 | 5/5 success。紧接 Flux.2 同卡切模。测试卡现为 L40S，L40S 耗时待补。 |

| 样本 | 秒 |
|---|---:|
| 青瓷壶 (从 Flux.2 切模) | 77.6s |
| 雨夜红风衣 | 54s |
| 梯田日出 | 53.9s |
| 窗边橘猫 | 53.9s |
| 青花瓷荔枝汽水 | 53.8s |

### Qwen-Image-2512 Lightning (`qwen-image-2512-lightning`)

LightX2V 8 步蒸馏 LoRA。比官方 50 步快约 6 倍，比 4 步/2 步 Turbo 更保画质。测试 L40S，正式 RTX-PRO-6000。

| 项 | 值 |
|---|---|
| 工作流 | `examples/qwen-image-2512-lightning.json` |
| 锁 | `examples/qwen-image-2512-lightning.lock.json` |
| 模式 | `workflow` |
| 量化 | fp8+lora |
| 节点 | core |
| 权重 | 34 GB；显存 — |
| 共用 | `qwen-image-2512` |
| 实测状态 | `hydrated` · PR #33 · 2026-08-14 |
| 实测 GPU / 耗时 | 已 hydrate，未记时 |
| 备注 | hydrate 已写入 8 步 LoRA。未排队出图。预期大约是 50 步的 1/6，待实测。 |

### Z-Image-Turbo (`z-image-turbo`)

蒸馏 8 步文生图。测试默认 L40S，正式推理 RTX-PRO-6000。CFG 固定 1。

| 项 | 值 |
|---|---|
| 工作流 | `examples/z-image-turbo-t2i.json` |
| 锁 | `examples/z-image-turbo-t2i.lock.json` |
| 模式 | `graph` |
| 量化 | bf16 |
| 节点 | core |
| 权重 | —；显存 — |
| 共用 | `z-image` |
| 实测状态 | `hydrated` · PR #32 · 2026-08-14 |
| 实测 GPU / 耗时 | 已 hydrate，未记时 |
| 备注 | hydrate 已写入 Volume。Ashley 0.32.0 无官方 subgraph，mode=graph。未排队出图。 |

## 文生视频

### Cosmos3-Edge (`cosmos3-edge`)

NVIDIA Cosmos 3 4B 边缘世界模型，文生视频（832×480 / 93 帧 / 24fps）。Nemotron-dense 骨干，ConvRot int4 约 3GB。提示词建议 JSON temporal_caption。测试 L40S，正式 RTX-PRO-6000。不要用 T4。

| 项 | 值 |
|---|---|
| 工作流 | `examples/cosmos3-edge-t2v.json` |
| 锁 | `examples/cosmos3-edge-t2v.lock.json` |
| 模式 | `workflow` |
| 量化 | int4-convrot |
| 节点 | ComfyUI-Cosmos3 |
| 权重 | 3.1 GB；显存 — |
| 共用 | — |
| 实测状态 | `recorded` · PR #35 · 2026-08-14 |
| 实测 GPU / 耗时 | L40S 49.1s |
| 冒烟产物 | 832×480 / 93 帧 / 24fps |

### Cosmos3-Nano (`cosmos3-nano`)

NVIDIA Cosmos 3 16B 全能世界模型，文生视频（832×480 / 93 帧 / 24fps）。Ashley 0.32.0 没有原生节点，用 RyukoMatoiFan/ComfyUI-Cosmos3。transformer 是 ConvRot int4（约 12GB），测试 L40S，正式 RTX-PRO-6000。不要用 T4。

| 项 | 值 |
|---|---|
| 工作流 | `examples/cosmos3-nano-t2v.json` |
| 锁 | `examples/cosmos3-nano-t2v.lock.json` |
| 模式 | `workflow` |
| 量化 | int4-convrot |
| 节点 | ComfyUI-Cosmos3 |
| 权重 | 12.4 GB；显存 7 GB；主机内存约 20 GB |
| 共用 | — |
| 实测状态 | `recorded` · PR #34 · 2026-08-14 |
| 实测 GPU / 耗时 | L40S 133.2s |
| 冒烟产物 | 832×480 / 93 帧 / 24fps |

### Cosmos3-Super (`cosmos3-super`)

NVIDIA Cosmos 3 64B 全能基座，文生视频（832×480 / 93 帧 / 24fps）。bf16 约 120GB，单卡放不下；本配方用 ConvRot int4（约 47GB）+ split_reasoner。测试 L40S，正式 RTX-PRO-6000。不要用 T4。

| 项 | 值 |
|---|---|
| 工作流 | `examples/cosmos3-super-t2v.json` |
| 锁 | `examples/cosmos3-super-t2v.lock.json` |
| 模式 | `workflow` |
| 量化 | int4-convrot |
| 节点 | ComfyUI-Cosmos3 |
| 权重 | 46.8 GB；显存 8 GB；主机内存约 63 GB |
| 共用 | `cosmos3-super-text2image` |
| 实测状态 | `pending` |
| 实测 GPU / 耗时 | 待补测 |
| 备注 | 与 Super T2I 同一套 int4。93 帧 SaveVideo 路径已由 Nano 验证；本配方尚未单独记时。 |

## 图生视频

### Cosmos3-Super-Image2Video (`cosmos3-super-image2video`)

64B 图生视频（832×480 / 93 帧 / 16fps）。transformer 是 ConvRot int4（约 47GB）+ split_reasoner。测试 L40S，正式 RTX-PRO-6000。不要用 T4。

| 项 | 值 |
|---|---|
| 工作流 | `examples/cosmos3-super-image2video.json` |
| 锁 | `examples/cosmos3-super-image2video.lock.json` |
| 模式 | `workflow` |
| 量化 | int4-convrot |
| 节点 | ComfyUI-Cosmos3 |
| 权重 | 46.7 GB；显存 8 GB；主机内存约 63 GB |
| 共用 | — |
| 实测状态 | `recorded` · PR #34 · 2026-08-14 |
| 实测 GPU / 耗时 | L40S 517.6s |
| 冒烟产物 | 832×480 / 93 帧 / 16fps |

### Cosmos3-Super-Image2Video-4Step (`cosmos3-super-image2video-4step`)

64B 图生视频 DMD2 蒸馏 4 步（832×480 / 93 帧 / 16fps）。euler + cfg 1.0 + distilled_4step，无需 CFG。transformer 是 ConvRot int4（约 47GB）+ split_reasoner。测试 L40S，正式 RTX-PRO-6000。不要用 T4。

| 项 | 值 |
|---|---|
| 工作流 | `examples/cosmos3-super-image2video-4step.json` |
| 锁 | `examples/cosmos3-super-image2video-4step.lock.json` |
| 模式 | `workflow` |
| 量化 | int4-convrot |
| 节点 | ComfyUI-Cosmos3 |
| 权重 | 46.7 GB；显存 8 GB；主机内存约 63 GB |
| 共用 | `cosmos3-super-text2image-4step` |
| 实测状态 | `recorded` · PR #35 · 2026-08-14 |
| 实测 GPU / 耗时 | L40S 50.8s |
| 冒烟产物 | 832×480 / 93 帧 / 16fps |

## 图生 3D

### Hunyuan3D 2.1 (`hunyuan3d-2.1`)

腾讯 Hunyuan3D 2.1 图生 GLB（几何）。官方 ComfyUI 核心节点，约 4.9GB 一体 checkpoint。测试 L40S，正式 RTX-PRO-6000。不要用 T4。

| 项 | 值 |
|---|---|
| 工作流 | `examples/hunyuan3d-2.1-image-to-3d.json` |
| 锁 | `examples/hunyuan3d-2.1-image-to-3d.lock.json` |
| 模式 | `workflow` |
| 量化 | — |
| 节点 | core |
| 权重 | 4.9 GB；显存 — |
| 共用 | — |
| 实测状态 | `recorded` · PR #37 · 2026-08-14 |
| 实测 GPU / 耗时 | L40S 43.9s |
| 冒烟产物 | GLB |
| 备注 | 合成物体图，导出 5.1MB GLB。 |

### Pixal3D (`pixal3d`)

图生 GLB。测试默认 L40S（约 20–32GB），正式推理 RTX-PRO-6000。不要用 T4。

| 项 | 值 |
|---|---|
| 工作流 | `examples/pixal3d-image-to-3d.json` |
| 锁 | `examples/pixal3d-image-to-3d.lock.json` |
| 模式 | `workflow` |
| 量化 | — |
| 节点 | Pixal3D-ComfyUI |
| 权重 | —；显存 32 GB |
| 共用 | — |
| 实测状态 | `pending` · PR #14 · 2026-08-13 |
| 实测 GPU / 耗时 | 待补测 |
| 备注 | gecko.jpg 跑通并导出 GLB，尚未记录墙钟秒数。 |

### TRELLIS.2 (`trellis2`)

Microsoft TRELLIS.2-4B 图生 GLB（几何）。visualbruno ComfyUI-Trellis2。效果依赖显卡，测试和正式都是 RTX-PRO-6000。不要用 T4 / L40S。

| 项 | 值 |
|---|---|
| 工作流 | `examples/trellis2-image-to-3d.json` |
| 锁 | `examples/trellis2-image-to-3d.lock.json` |
| 模式 | `workflow` |
| 量化 | bf16 |
| 节点 | ComfyUI-Trellis2 |
| 权重 | 16 GB；显存 — |
| 共用 | — |
| 实测状态 | `recorded` · PR #37 · 2026-08-14 |
| 实测 GPU / 耗时 | RTX-PRO-6000 208.3s |
| 冒烟产物 | GLB |
| 备注 | 合成物体图，导出 8.7MB GLB。attention sdpa，preprocess 开 rembg。 |

### TripoSplat (`triposplat`)

图生 Gaussian Splat。测试默认 L40S（约 48GB），正式推理 RTX-PRO-6000。

| 项 | 值 |
|---|---|
| 工作流 | `examples/triposplat-image-to-gaussian-splat.json` |
| 锁 | `examples/triposplat-image-to-gaussian-splat.lock.json` |
| 模式 | `workflow` |
| 量化 | fp16 |
| 节点 | core |
| 权重 | —；显存 48 GB |
| 共用 | — |
| 实测状态 | `recorded` · PR #13 · 2026-08-13 |
| 实测 GPU / 耗时 | L40S 首张 53.7s / 热 30.1s |
| 备注 | 3/3 success。耗时随物体变化 30–54s。 |

| 样本 | 秒 |
|---|---:|
| headphones | 53.7s |
| wristwatch | 30.1s |
| rubber-duck | 32.8s |

## 怎么更新

1. 加 `examples/*.json` + `*.lock.json` + `catalog/<id>.json`。
2. 在 `benchmarks/models.json` 加同一 `id`（权重说明、节点、`smoke.status`）。
3. 冒烟成功后把 `status` 改成 `recorded`，填 `gpu` / `seconds` / `source`。
4. `python3 -m benchmarks --write` 重写本页。
5. **停掉** `modal serve`。

`smoke.status`：`recorded` 已记墙钟；`hydrated` 权重在 Volume 但没排队；`pending` 还没测。
